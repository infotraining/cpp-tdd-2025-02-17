#ifndef SNAKE_HPP
#define SNAKE_HPP

#include <algorithm>
#include <array>
#include <cassert>
#include <iostream>
#include <random>
#include <string>
#include <thread>
#include <vector>
#include <optional>
#include <functional>

enum class Key {
    P,
    Q,
    ArrowUp,
    ArrowDown,
    ArrowLeft,
    ArrowRight
};

enum class Direction {
    Up,
    Down,
    Left,
    Right
};

#endif